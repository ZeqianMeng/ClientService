package service;

import org.restlet.resource.ServerResource;

public class CompleteServiceResource extends ServerResource{
	//if Junyi's code cannot tell which resource completes (contract_id), pass job_id, resource, stop_time here
	//to get contract_id from job_data in SteerServiceServer

}
